/**
 * Toggle off when the real driver analytics API is ready; implement fetch in
 * `src/services/driverEarningsService.ts` and keep return type `DriverEarningsDashboard`.
 */
export const USE_DUMMY_DRIVER_EARNINGS = true;
